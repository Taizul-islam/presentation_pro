name: Build Windows EXE

on:
  push:
    branches: [ main, master ]
  workflow_dispatch:

jobs:
  build-windows:
    runs-on: windows-latest
    permissions:
      contents: write

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.44.7'
          channel: 'stable'
          cache: true

      - name: Enable Windows support
        run: flutter config --enable-windows-desktop

      - name: Get dependencies
        run: flutter pub get

      - name: Build Windows EXE
        run: flutter build windows --release

      - name: Install LibreOffice via winget
        shell: pwsh
        run: |
          winget install --id TheDocumentFoundation.LibreOffice -e --accept-package-agreements --accept-source-agreements

      - name: Find and Copy LibreOffice
        shell: pwsh
        run: |
          $buildDir = "build\windows\x64\runner\Release"
          $loDir = "$buildDir\tools\libreoffice"
          New-Item -ItemType Directory -Path $loDir -Force
          
          # Find LibreOffice installation
          $possiblePaths = @(
            "C:\Program Files\LibreOffice",
            "C:\Program Files (x86)\LibreOffice"
          )
          
          $found = $false
          foreach ($path in $possiblePaths) {
            if (Test-Path "$path\program\soffice.exe") {
              Write-Host "Found LibreOffice at: $path"
              Copy-Item -Path "$path\*" -Destination $loDir -Recurse -Force
              $found = $true
              break
            }
          }
          
          if (-not $found) {
            Write-Host "ERROR: LibreOffice not found after winget install"
            exit 1
          }
          
          Write-Host "LibreOffice copied to: $loDir"

      - name: Download Poppler
        shell: pwsh
        run: |
          $url = "https://github.com/oschwartz10612/poppler-windows/releases/download/v24.08.0-0/Release-24.08.0-0.zip"
          Invoke-WebRequest -Uri $url -OutFile "poppler.zip"

      - name: Extract Poppler
        shell: pwsh
        run: |
          Expand-Archive -Path "poppler.zip" -DestinationPath "poppler_extracted" -Force
          $popplerDir = Get-ChildItem "poppler_extracted" -Directory | Select-Object -First 1
          $binDir = Get-ChildItem $popplerDir.FullName -Recurse -Directory -Filter "bin" | Select-Object -First 1
          $destDir = "build\windows\x64\runner\Release\tools\poppler"
          New-Item -ItemType Directory -Path $destDir -Force
          Copy-Item -Path "$($binDir.FullName)\*" -Destination $destDir -Recurse -Force

      - name: Copy pdfium.dll
        shell: pwsh
        run: |
          $buildDir = "build\windows\x64\runner\Release"
          if (-not (Test-Path "$buildDir\pdfium.dll")) {
            $pdfium = Get-ChildItem $buildDir -Recurse -Filter "pdfium.dll" -ErrorAction SilentlyContinue | Select-Object -First 1
            if ($pdfium) {
              Copy-Item $pdfium.FullName -Destination "$buildDir\" -Force
            }
          }

      - name: Verify files
        shell: pwsh
        run: |
          $buildDir = "build\windows\x64\runner\Release"
          if (-not (Test-Path "$buildDir\mobile.exe")) { Write-Host "FAIL: mobile.exe"; exit 1 }
          if (-not (Test-Path "$buildDir\pdfium.dll")) { Write-Host "FAIL: pdfium.dll"; exit 1 }
          
          $soffice = Get-ChildItem "$buildDir\tools" -Recurse -Filter "soffice.exe" -ErrorAction SilentlyContinue | Select-Object -First 1
          $pdftoppm = Get-ChildItem "$buildDir\tools" -Recurse -Filter "pdftoppm.exe" -ErrorAction SilentlyContinue | Select-Object -First 1
          
          if (-not $soffice) { Write-Host "FAIL: soffice.exe"; exit 1 }
          if (-not $pdftoppm) { Write-Host "FAIL: pdftoppm.exe"; exit 1 }
          
          Write-Host "All files verified."

      - name: Create ZIP
        shell: pwsh
        run: |
          Compress-Archive -Path "build\windows\x64\runner\Release\*" -DestinationPath "PresentationPro_Portable.zip"

      - name: Upload Build Artifacts
        uses: actions/upload-artifact@v4
        with:
          name: presentation-pro-portable
          path: "PresentationPro_Portable.zip"
          retention-days: 30