name: mobile
description: Professional Presentation Painting Tool

publish_to: 'none'

version: 1.0.1+2

environment:
  sdk: ^3.12.2

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.9
  shared_preferences:
  flutter_svg:
  google_fonts:
  provider:
  file_picker:
  pdfrx:
  path_provider:
  archive:
  xml:
  pdf:
  printing:
  window_manager:

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true